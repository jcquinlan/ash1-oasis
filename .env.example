# PostgreSQL Configuration
POSTGRES_USER=oasis
POSTGRES_PASSWORD=changeme
POSTGRES_DB=oasis

# Database connection string for services
DATABASE_URL=postgresql://oasis:changeme@oasis:5432/oasis

# Anthropic API key (for project step generation)
ANTHROPIC_API_KEY=

# Better Auth — generate secret with: openssl rand -base64 32
# WARNING: The app will refuse to start with the default placeholder value.
BETTER_AUTH_SECRET=change-me-generate-a-real-secret
BETTER_AUTH_URL=http://localhost:3001

# Allowed origins for CORS (comma-separated)
# In production, set this to your actual domain(s)
ALLOWED_ORIGINS=http://localhost:3000

# Environment
NODE_ENV=development
