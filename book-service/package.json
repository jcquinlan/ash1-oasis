{
  "name": "book-service",
  "version": "1.0.0",
  "description": "Book recommendation service with availability checking",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "cli": "bun run src/cli.ts",
    "deploy": "bun build --target=bun --outfile=./dest/server.js ./src/index.ts && bun run ./dest/server.js"
  },
  "dependencies": {
    "hono": "^4.11.4",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
